<form method="post" action="{{ path('app_xbox_delete', {'id': xbox.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ xbox.id) }}">
    <button class="btn">Delete</button>
</form>
